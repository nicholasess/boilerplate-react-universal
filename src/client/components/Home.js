import React from 'react'

export default () => (
	<div style={styles.container}>
		<div>Home component</div>
	</div>
)

const styles = {
	container: {
		display: 'flex',
		flexDirection: 'row',
		alignItems: 'center',
		width: '1070px',
		margin: 'auto'

	}
}